org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# This newDsl flag was added by the Flutter template
android.newDsl=false
# This builtInKotlin flag was added by the Flutter template. While it is false,
# plugins that support AGP 9's built-in Kotlin (flutter_timezone,
# flutter_volume_controller) apply the old Kotlin Gradle Plugin themselves,
# which is what the build warns about. Turning it to true is the fix, once the
# whole build (including integration_test) is checked with it; see README
# "Build health".
android.builtInKotlin=false
