<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.VIBRATE"/>
    <!-- Voice counting (record): the microphone is analysed on the device and
         never uploaded. INTERNET is only for downloading Google Fonts. -->
    <uses-permission android:name="android.permission.RECORD_AUDIO"/>
    <uses-permission android:name="android.permission.INTERNET"/>
    <!-- Alarms and reminders (flutter_local_notifications): show notifications
         (Android 13+), keep them across a reboot, and ring at the exact second.
         Sadho is an alarm-clock app (the sun-based alarm, the Clock timer, the
         Sadhana finish alarm) and sends calendar event reminders, which is what
         Google Play allows USE_EXACT_ALARM for: it is granted automatically on
         Android 13+. Android 12 uses SCHEDULE_EXACT_ALARM instead. If exact
         alarms are ever not allowed, the app falls back to a slightly flexible
         time and says so. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>
    <uses-permission android:name="android.permission.USE_EXACT_ALARM"/>
    <uses-permission
        android:name="android.permission.SCHEDULE_EXACT_ALARM"
        android:maxSdkVersion="32"/>
    <!-- Sadhana completion alarm: shows full screen over the lock screen,
         like an alarm clock (Android 14+ lets the user refuse it; the alarm
         is then a normal heads-up notification). -->
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT"/>
    <!-- Keeps the screen on while a Sadhana session is counting (optional
         setting, wakelock_plus). -->
    <uses-permission android:name="android.permission.WAKE_LOCK"/>
    <!-- Sun-based alarm: sunrise/sunset need only an approximate (city-level)
         position, so only COARSE location is requested. It is used on the
         device and never uploaded. -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
    <!-- Mala with the screen off (MalaCounterService): a foreground service
         that runs only while a Mala session is running, started when the user
         presses Start. It counts hardware volume-key presses through a
         MediaSession with remote volume (no audio is played) and rings at the
         target. specialUse, declared in the Play Console. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE"/>
    <!-- Quiet mode during sadhana (optional, off by default): sets Do Not
         Disturb to alarms-only while a session runs and restores it after.
         The user grants this access in the phone's settings; Sadho never
         changes Do Not Disturb without it. -->
    <uses-permission android:name="android.permission.ACCESS_NOTIFICATION_POLICY"/>
    <application
        android:label="Sadho"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <service
            android:name=".MalaCounterService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Counts mala repetitions from hardware volume-key presses while the screen is off, and rings when the user's target is reached (hands-free japa counter)."/>
        </service>
        <receiver android:exported="false" android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationReceiver" />
        <receiver android:exported="false" android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationBootReceiver">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED"/>
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED"/>
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
                <action android:name="com.htc.intent.action.QUICKBOOT_POWERON"/>
            </intent-filter>
        </receiver>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
